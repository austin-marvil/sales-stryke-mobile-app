import { EnumBase } from '../../lang/EnumBase';
import { AgreementType } from '../classes/persistable/AgreementType';
export declare class AgreementTypeEnum extends EnumBase<AgreementTypeEnum> {
    static BULK_PICKUP: AgreementTypeEnum;
    static DISPOSAL_GUIDE: AgreementTypeEnum;
    static WORKSPACE: AgreementTypeEnum;
    static ORGANIZATION: AgreementTypeEnum;
    static GENERAL: AgreementTypeEnum;
    private agreementType;
    protected constructor(p: {
        databaseUUID: string;
        description: string;
        enumId: number;
        name: string;
    });
    getAgreementType(): AgreementType;
}
