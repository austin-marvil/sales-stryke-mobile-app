import { EnumBase } from '../../lang/EnumBase';
import { UserType } from '../classes/persistable/UserType';
export declare class UserTypeEnum extends EnumBase<UserTypeEnum> {
    static Customer: UserTypeEnum;
    static Employee: UserTypeEnum;
    static Client: UserTypeEnum;
    private userType;
    protected constructor(p: {
        databaseUUID: string;
        description: string;
        name: string;
    });
    getUserType(): UserType;
}
