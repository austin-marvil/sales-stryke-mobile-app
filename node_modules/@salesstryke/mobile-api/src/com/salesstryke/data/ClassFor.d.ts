declare class MetaData {
    namespace: string;
}
export declare class ClassFor {
    static metaDataForClassFor: MetaData;
    private static INSTANCE;
    private targetClass;
    private constructor();
    static getInstance(): ClassFor;
    addClass(namespace: string, targetClass: any): ClassFor;
    /**
     * Returns the target class if found, otherwise null.
     * @param namespace
     * @returns
     */
    getClass(namespace: string): any;
    getNew(namespace: string): any;
}
export {};
