import { RelationshipType } from "./RelationshipType";
export declare class RelationshipBase {
    private description_s;
    private relationshipType;
    /**
     * When creating a data model, the EntityModel may have data relationships defined: ManyToOne, OneToMany, etc. It MUST have a UUID assigned
     * to it. That is THIS relationship.
     */
    private relationshipUUID_s;
    constructor(relationshipType: RelationshipType);
    getDescription(): string;
    getHashCode(): number;
    getRelationshipType(): RelationshipType;
    getRelationshipUUID(): string | null;
    setDescription(description_s: string): RelationshipBase;
    setRelationshipUUID(relationshipUUID_s: string): RelationshipBase;
}
