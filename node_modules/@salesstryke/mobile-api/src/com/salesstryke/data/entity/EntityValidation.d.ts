import { JsonApiBase } from "../../jsonApi/JsonApiBase";
import { JsonApiError } from "../../jsonApi/JsonApiError";
import { AttributeValidationError } from "./errors/AttributeValidationError";
export declare class EntityValidation {
    #private;
    private errors_ary;
    private validationErrors;
    getErrors(): Array<JsonApiError>;
    getValidationErrors(): Array<AttributeValidationError>;
    isValid(): boolean;
    /**
     * Validates root object, and all data relationships: MANY_TO_MANY, MANY_TO_ONE, ONE_TO_MANY, ONE_TO_ONE
     * @param jsonApiPersistableBase
     * @returns
     */
    private _isValid;
    validate(jsonApiBase: JsonApiBase): EntityValidation;
}
