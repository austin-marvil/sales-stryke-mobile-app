export declare class NamedBinding {
    private alias;
    private propertyName;
    constructor(p: {
        propertyName: string;
        alias?: string;
    });
    getAlias(): string | undefined;
    getPropertyName(): string;
}
export declare class Import {
    private moduleSpecifier;
    private namedBindings;
    /**
     * ```
     * Given the import statement: import { Foo, Bar as Baz } from "./myModule";
     *
     * "./myModule" is the Module Specifier
     * { Foo, Bar as Baz } represents Named Bindings:
     * Foo is imported directly by its name.
     * Bar is imported and aliased locally to Baz.
     *
     * ```
     * @param p
     */
    constructor(p: {
        moduleSpecifier: string;
        namedBindings: Array<NamedBinding>;
    });
    getModuleSpecifier(): string;
    getNamedBindings(): Array<NamedBinding>;
}
