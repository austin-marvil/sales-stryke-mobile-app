"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ClassFor = void 0;
class MetaData {
    constructor() {
        this.namespace = 'com.salesstryke.data.ClassFor';
    }
}
class ClassFor {
    constructor() {
        this.targetClass = new Map();
    }
    static getInstance() {
        return (ClassFor.INSTANCE);
    }
    addClass(namespace, targetClass) {
        this.targetClass.set(namespace, targetClass);
        return (this);
    }
    /**
     * Returns the target class if found, otherwise null.
     * @param namespace
     * @returns
     */
    getClass(namespace) {
        let clazz = this.targetClass.get(namespace);
        if (clazz === undefined) {
            clazz = null;
        }
        return (clazz);
    }
    getNew(namespace) {
        let newInstance = null;
        let clazz = this.getClass(namespace);
        if (clazz !== null) {
            newInstance = new clazz();
        }
        return (newInstance);
    }
}
exports.ClassFor = ClassFor;
ClassFor.metaDataForClassFor = new MetaData();
ClassFor.INSTANCE = new ClassFor();
//# sourceMappingURL=ClassFor.js.map