"use strict";
var _a, _b;
Object.defineProperty(exports, "__esModule", { value: true });
exports.port = exports.domain = exports.scheme = void 0;
let scheme = 'http';
exports.scheme = scheme;
let domain = 'localhost';
exports.domain = domain;
let port = 8080;
exports.port = port;
const API_ENDPOINT = (_b = (_a = process.env.API_ENDPOINT) !== null && _a !== void 0 ? _a : process.env.REACT_APP_API_ENDPOINT) !== null && _b !== void 0 ? _b : '__INJECT_API_ENDPOINT__';
if (API_ENDPOINT && API_ENDPOINT !== '__INJECT_API_ENDPOINT__') {
    // Use a regex to extract the schema, domain, and port (if present)
    const regex = /^(https?):\/\/([^/:]+)(?::(\d+))?/;
    const match = API_ENDPOINT.match(regex);
    if (match) {
        exports.scheme = scheme = match[1];
        exports.domain = domain = match[2];
        exports.port = port = parseInt(match[3], 10);
        if (!port) {
            exports.port = port = scheme === 'https' ? 443 : 80;
        }
    }
}
//# sourceMappingURL=ServerApi.config.js.map