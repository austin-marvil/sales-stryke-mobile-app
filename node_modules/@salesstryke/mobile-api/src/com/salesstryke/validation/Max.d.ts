import { ValidationBase } from "./ValidationBase";
export declare class Max extends ValidationBase {
    private max;
    constructor(p: {
        max: number;
    });
    getClone(): Max;
    getMax(): number;
    isValid(): boolean;
}
