import { Token } from "./Token";
export declare class RuleEngine {
    static comparators: Array<Token>;
    private attributes;
    private tokens;
    constructor(p: {
        attributes: Array<Token>;
    });
    addToken(p: {
        token: Token;
    }): RuleEngine;
    getAttributes(): Array<Token>;
    static getComparators(): Array<Token>;
    getComparators(): Array<Token>;
    getTokens(): Array<Token>;
}
