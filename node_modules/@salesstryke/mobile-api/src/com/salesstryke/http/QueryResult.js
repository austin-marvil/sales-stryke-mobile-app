"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.QueryResult = void 0;
// TODO: review => are we using it properly? => DELETE_FILE?
class QueryResult {
    constructor() {
        this.data_japb_ary = new Array();
        this.recordSize_i = 0;
    }
    getData() {
        let data_japb_ary = this.data_japb_ary;
        return (data_japb_ary);
    }
    /**
     * Returns the total record size even if the query is returning a single page.
     * @returns
     */
    getRecordSize() {
        return (this.recordSize_i);
    }
    setData(data_japb_ary) {
        this.data_japb_ary = data_japb_ary;
        return (this);
    }
    setRecordSize(recordSize_i) {
        this.recordSize_i = recordSize_i;
        return (this);
    }
}
exports.QueryResult = QueryResult;
//# sourceMappingURL=QueryResult.js.map