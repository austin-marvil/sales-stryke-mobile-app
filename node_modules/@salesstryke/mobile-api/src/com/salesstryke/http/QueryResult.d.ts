export declare class QueryResult {
    private data_japb_ary;
    private recordSize_i;
    getData<T>(): Array<T>;
    /**
     * Returns the total record size even if the query is returning a single page.
     * @returns
     */
    getRecordSize(): number;
    setData<T>(data_japb_ary: Array<T>): QueryResult;
    setRecordSize(recordSize_i: number): QueryResult;
}
