import { JSONArray } from "./JSONArray";
export declare class JSONObject {
    #private;
    private json_s;
    private jsonObject_map;
    constructor(json_any?: any);
    private toMap;
    /**
     * If not found returns null.
     * @param key_s
     * @returns
     */
    get(key_s: string): any;
    getBoolean(key_s: string, default_b?: boolean): boolean;
    getJSONArray(key_s: string): JSONArray | null;
    getJSONObject(key_s: string): JSONObject | null;
    getNames(): Array<string>;
    getNumber(key_s: string, default_n?: number): number;
    getString(key_s: string): string | null;
    put(key_s: string | null, value_any: any): JSONObject;
    size(): number;
    private quote;
    toString(): string;
    private write;
}
