import { QueryParamBase } from "../QueryParamBase";
export declare class QueryParam extends QueryParamBase {
    private static entityType;
    private value;
    /**
     *
     * @param name_s
     * @param value
     */
    constructor(name_s: string, dataType: number, value: string | number | boolean | Date);
    getValue(): string | number | boolean | Date;
    /**
     * will return 0 if its NAN
     * @returns number
     */
    getValueAsNumber(): number;
    /**
     * will return '' if its not a string
     * @returns string
     */
    getValueAsString(): string;
    /**
     * will return false if its not a boolean
     * @returns boolean
     */
    getValueAsBoolean(): boolean;
    /**
     * will return a new Date if its not a Date
     * @returns boolean
     */
    getValueAsDate(): Date;
    setValue(value: string | number | boolean | Date): void;
}
