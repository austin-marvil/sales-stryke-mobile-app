"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.JsonApiUtils = void 0;
const IsArray_1 = require("../utils/object/IsArray");
const JsonApiError_1 = require("./JsonApiError");
class JsonApiUtils {
    static firstCharToLowerCase(value) {
        let result = value.substring(0, 1).toLocaleUpperCase() + value.substring(1);
        return (result);
    }
    static firstCharToUpperCase(value) {
        let result = value.substring(0, 1).toLocaleUpperCase() + value.substring(1);
        return (result);
    }
    static getSimpleNameFromNamespace(namespace) {
        let simpleName = "";
        let index = namespace.lastIndexOf(".");
        if (index != -1) {
            simpleName = namespace.substring(index + 1);
        }
        else {
            simpleName = namespace;
        }
        return (simpleName);
    }
    static isJsonApiErrorArray(value_any) {
        let isJsonApiErrorArray_b = false;
        if ((0, IsArray_1.IsArray)(value_any)) {
            let array_any = value_any;
            let allJsonApiError_b = true;
            let size_i = array_any.length;
            for (let index_i = 0; index_i < size_i; index_i++) {
                let item_any = array_any[index_i];
                if (!(item_any instanceof JsonApiError_1.JsonApiError)) {
                    allJsonApiError_b = false;
                    break;
                }
            }
            if (allJsonApiError_b) {
                isJsonApiErrorArray_b = true;
            }
        }
        return (isJsonApiErrorArray_b);
    }
}
exports.JsonApiUtils = JsonApiUtils;
//# sourceMappingURL=JsonApiUtils.js.map