import { JsonApiError } from "./JsonApiError";
import { JsonApiQuery } from "./JsonApiQuery";
import { ResourceObject } from "./ResourceObject";
export declare class JsonApiDocument {
    private data;
    private errors;
    private query;
    addError(jsonApiError: JsonApiError): JsonApiDocument;
    addData(dataItem: any): JsonApiDocument;
    /**
     * Returns all data as Array<ResourceObject>;
     * @returns
     */
    getDataItems(): Array<ResourceObject>;
    /**
     * Returns all data as Array<Object>;
     * @returns
     */
    getData(): Array<object>;
    /**
     * Returns an Array of the Entity type.
     * @returns
     */
    getDataAs<T>(): Array<T>;
    /**
     * Returns single Entity.
     * @returns
     */
    getDataAsItem<T>(): T | null;
    getErrors(): Array<JsonApiError>;
    getQuery(): JsonApiQuery | null;
    setQuery(jsonApiQuery: JsonApiQuery): JsonApiDocument;
}
