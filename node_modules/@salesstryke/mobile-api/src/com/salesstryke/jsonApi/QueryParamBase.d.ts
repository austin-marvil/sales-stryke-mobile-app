export declare class QueryParamBase {
    private dataType;
    private entityType;
    private name;
    constructor(name: string, dataType: number);
    protected setEntityType(entityType: string): QueryParamBase;
    getEntityType(): string;
    getDataType(): number;
    getName(): string;
}
