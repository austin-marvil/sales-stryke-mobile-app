export declare class Timer extends Object {
    private end_dt;
    private start_dt;
    constructor();
    end(): Timer;
    /**
     * ```
     * Returns the difference between start and end in milliseconds. -1 if .start not called. will call .end if not called.
     * type TGetTime =
     * {
     *     newEnd? : boolean; // force set new end time.
     * }
     * ```
     */
    getTime(p?: TGetTime): number;
    private init;
    start(): Timer;
}
export type TGetTime = {
    newEnd?: boolean;
};
