import { JsonApiBase } from "../jsonApi";
export declare class Clone {
    /**
     * Not suitable to clone an Entity.
     * @param p
     * @returns
     */
    static shallowClone<T>(p: {
        object: any;
    }): T;
    static cloneEntity<T>(p: {
        entity: JsonApiBase;
    }): T;
    static cloneEntityArray<T>(p: {
        jsonApiBase: Array<JsonApiBase>;
    }): Array<T>;
}
