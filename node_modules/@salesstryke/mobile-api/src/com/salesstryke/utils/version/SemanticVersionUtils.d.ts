export declare class SemanticVersionUtils {
    /**
     * ```
     * Takes a version number as a string and returns a number.
     *
     * 00.00.00
     *       patch
     *    minor
     * major
     *
     * major, minor, patch will be 0 padded 2
     *
     * .1 -> .10
     *
     * 01.4.01 -> 14001
     * 01.4.1  -> 14010
     *
     *
     * ```
     * @param version
     * @returns
     */
    static versionToNumber(version: string): number;
}
