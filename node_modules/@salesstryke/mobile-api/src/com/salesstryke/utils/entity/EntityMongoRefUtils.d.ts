import { JsonApiPersistableBase } from "../../jsonApi";
export declare class EntityMongoRefUtils {
    static updateMongoRefConnectorItems<T extends JsonApiPersistableBase, C extends JsonApiPersistableBase>(p: {
        existingItems: C[];
        newItems: T[];
        createAction: (item: T) => void;
        deleteAction: (item: C) => void;
        compareAction: (existingItem: C, newItem: T) => boolean;
    }): void;
}
