import { SecureIdentity } from "../../entity/classes/persistable/SecureIdentity";
import { SecurityPermissionEnum } from "../../entity/enums/SecurityPermissionEnum";
export declare class SecurePermissionUtils {
    /**
     * ```
     * requiredPermissions - The requrired permissions. Only one need to match!
     * secureIdentity      - The SecureIdentity that is check against for the required permission
     * ```
     */
    static hasSecurityPermission(p: {
        requiredPermissions: SecurityPermissionEnum[];
        secureIdentity: SecureIdentity;
    }): boolean;
    private static hasRequriedSecurityPermission;
    private static hasSecurityPermissonInSecurityPermissionList;
    private static hasSecurityPermissonInSecurityRoleList;
    private static isRootUser;
}
