import { ImportDataFileUpload, ImportDataMap } from '../../entity/classes/persistable';
interface IUploadFile {
    file: File;
    importDataMap: ImportDataMap;
    onProgress?: (percentage: number) => void;
    onFailed?: () => void;
    loader?: (boolean: boolean) => void;
}
/**
 * UploadFile
 * @param {object} {file: FileList,} - See type of endpoint param
 */
export declare function s3UploadFile({ file, importDataMap, onProgress, onFailed, loader }: IUploadFile): Promise<ImportDataFileUpload | null>;
export {};
