"use strict";
// TODO: currently use for softpak... combine it wiith Date Util later
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseDateToYYYYMMDDHHMM = exports.parseDateToYYMMDD = exports.parseDateToYYYYMMDD = void 0;
const parseDateToYYYYMMDD = (date) => {
    if (date === null) {
        return 11111111;
    }
    let now = new Date(date);
    let y = now.getFullYear();
    let m = now.getMonth() + 1;
    let d = now.getDate();
    return Number('' + y + (m < 10 ? '0' : '') + m + (d < 10 ? '0' : '') + d);
};
exports.parseDateToYYYYMMDD = parseDateToYYYYMMDD;
const parseDateToYYMMDD = (date) => {
    if (date === null) {
        return 111111;
    }
    let now = new Date(date);
    let y = now.getFullYear();
    let m = now.getMonth() + 1;
    let d = now.getDate();
    return Number('' + y.toString().slice(2) + (m < 10 ? '0' : '') + m + (d < 10 ? '0' : '') + d);
};
exports.parseDateToYYMMDD = parseDateToYYMMDD;
const parseDateToYYYYMMDDHHMM = (date) => {
    if (date === null) {
        return 11111111;
    }
    let now = new Date(date);
    let y = now.getFullYear();
    let m = now.getMonth() + 1;
    let d = now.getDate();
    let h = now.getHours();
    let mm = now.getMinutes();
    return Number('' + y + (m < 10 ? '0' : '') + m + (d < 10 ? '0' : '') + d + (h < 10 ? '0' : '') + h + (mm < 10 ? '0' : '') + mm);
};
exports.parseDateToYYYYMMDDHHMM = parseDateToYYYYMMDDHHMM;
//# sourceMappingURL=DateConversionUtil.js.map