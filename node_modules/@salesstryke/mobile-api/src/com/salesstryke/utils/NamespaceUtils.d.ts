export declare class NamespaceUtils {
    static eatFirstSubNamespace(namespace_s: string): string;
    /**
     * Give 'com.foo.bar', returns 'com'.
     * @param namespace_s
     * @returns
     */
    static getFirstSubNamespaceName(namespace_s: string): string;
    /**
     * com.foo.Bar returns 2, com and foo are subnamespaces, Bar is the ClassName.
     * @param namespace_s
     * @returns
     */
    static getSubNamespaceLevelCount(namespace_s: string): number;
    /**
     * 'com.some.path.Foo' returns 'com.some.path'
     * @param namespace
     * @returns
     */
    static namespaceToPackageName(namespace: string): string;
    /**
     * 'com.some.path.Foo' returns 'Foo'
     * @param namespace
     * @returns
     */
    static namespaceToSimpleName(namespace: string): string;
}
