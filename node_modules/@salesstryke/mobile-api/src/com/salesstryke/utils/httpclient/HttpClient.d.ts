import { JsonApiDocument } from "../../jsonApi/JsonApiDocument";
import { HttpMethodType } from "../../http/HttpMethodType";
export declare class HttpClient {
    #private;
    private baseURI;
    private config;
    private domain;
    /**
     * A config item.
     */
    private isAuthenticated_b;
    private jwtToken;
    private port;
    private scheme;
    /**
     *
     * @param scheme
     * @param domain
     * @param port
     */
    constructor(scheme: string, domain: string, port: number);
    call(httpMethodType: HttpMethodType, path_s: string, jsonApiDocument: JsonApiDocument): Promise<JsonApiDocument>;
    getIsAuthenticated(): boolean;
    private getJsonApiErrorsAsErrors;
    getJwtToken(): string;
    logout(): void;
    setIsAuthenticated(isAuthenticated_b: boolean): HttpClient;
    setJwtToken(jwtToken: string): HttpClient;
    private updateConfig;
}
