export declare class StringUtils {
    static firstCharToLowerCase(value: string): string;
    static firstCharToUpperCase(value: string): string;
    static capitalizeFirstLetter(value: string): string;
    static capitalizeWordsFirstLetter(value: string): string;
    static getAsQuotedString(value_s: string): string;
    static camelCaseToAllUpperCaseSeparatedByUnderscore(p: {
        value: string;
    }): string;
    static escapeRegExp(value: string): string;
    static replaceAll(str: string, find: string, replace: string): string;
    static spacePascalCase(value_s: string): string;
    static snakeCase(value_s: string): string;
    static nullStringIsEmptyString(value: string | null): string;
    /**
     * Cleans a string so it is safe for use as a value in a JSON object.
     * Removes control characters, trims whitespace, and replaces problematic unicode.
     * Ensures the string can be safely stringified and parsed as JSON.
     */
    static cleanForJson(value: string | null | undefined): string;
}
