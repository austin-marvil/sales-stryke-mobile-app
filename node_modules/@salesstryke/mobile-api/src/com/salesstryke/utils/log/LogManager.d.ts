import { Logger } from './Logger';
import { Level } from './LogLevel';
type TSetLogLevel = {
    level: Level;
    applyToAllLoggers?: boolean;
};
declare class LogManager {
    private static INSTANCE;
    private loggers;
    private logLevel;
    private constructor();
    static getInstance(): LogManager;
    getLogger(namespace: string): Logger;
    getLoggingLevel(): Level;
    setLogLevel({ level, applyToAllLoggers }: TSetLogLevel): LogManager;
}
declare const logManager: LogManager;
export { logManager as LogManager };
