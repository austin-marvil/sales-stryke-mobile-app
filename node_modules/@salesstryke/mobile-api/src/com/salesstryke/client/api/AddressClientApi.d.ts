import { Address } from "../../entity/classes/persistable";
import { HttpClient } from "../../utils/httpclient";
import { ApiBase } from "./ApiBase";
export declare class AddressClientApi extends ApiBase {
    constructor(httpClient: HttpClient);
    delete(p: {
        request: Address;
    }): Promise<Address | null>;
    save(request: Address): Promise<Address | null>;
    getById(request: Address): Promise<Address | null>;
    getGeocoder(request: Address): Promise<Array<Address> | null>;
}
